{
  "rules": {
    ".read": true,
    "contact": {
    	".read": true,
    	".write": true
    },
		"blog" : {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}
